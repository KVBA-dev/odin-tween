# odin-tween

This is a simple library implementing tweening for values. It supports all the float types, vector types, and raylib colours.

To include this into your project, clone this repository and copy+paste the `tween` directory to your project.
